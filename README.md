Fraud detection platform on the IEEE-CIS dataset. Full README lands in stage 10.

Demo: `docs/demo/index.html`, a self-contained replay of the stage 9 lifecycle run built from
the committed artifacts by `make demo-page`.
