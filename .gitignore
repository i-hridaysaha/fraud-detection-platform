# data is Vesta-provided under Kaggle competition rules and is not redistributable
data/
*.csv
*.csv.zip

# environments
.venv/
venv/
.env

# build and cache
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
*.egg-info/
build/
dist/

# generated artifacts that are large or rebuildable
reports/*.parquet
reports/cache/
mlruns/

# macos
.DS_Store

# the shipped model binary is rebuilt by `make train`; the bundle beside it is committed
models/*.ubj
models/*.pkl

# the load test server log is rewritten by every run
loadtest/server.log
